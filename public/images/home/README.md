Add a local homepage hero image here:

public/images/home/hero.jpg

The homepage already has a premium gradient and external-image fallback, so the
site will not show a plain black hero if this file is missing.
