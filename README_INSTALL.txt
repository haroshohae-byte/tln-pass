Fixes ESLint react-hooks/set-state-in-effect in app/join/JoinCheckoutForm.tsx.
Copy the app folder into the project root and replace files.
Then run:
  rmdir /s /q .next
  npm.cmd run lint
  npm.cmd run build
